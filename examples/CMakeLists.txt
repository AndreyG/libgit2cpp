file(GLOB_RECURSE LIBGIT2CPP_EXAMPLE_SOURCES *.cpp)
foreach(EXAMPLE_SOURCE ${LIBGIT2CPP_EXAMPLE_SOURCES})
	get_filename_component(EXAMPLE_NAME ${EXAMPLE_SOURCE} NAME_WE)
	add_executable(${EXAMPLE_NAME}-cpp ${EXAMPLE_SOURCE})
	target_link_libraries(${EXAMPLE_NAME}-cpp ${package})
endforeach()
